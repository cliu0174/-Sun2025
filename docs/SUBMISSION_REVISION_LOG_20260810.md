# PI-MSCL 投稿稿修订记录（2026-08-10）

## 导师／审稿人判断

本轮修订后，论文的主动线已明确为：**受控的稀疏循环级标签条件下，软单调先验如何在跨电芯 SOH 估计中改变点误差与轨迹一致性之间的关系。**

稿件不再将随机保留标签表述为 BMS 自然缺失、连续前缀外推或周期性标定；这些问题可作为后续验证协议，而非本研究已证明的结论。全文的结果陈述以同协议、配对比较为准，避免以单一最优指标宣称普遍优势。

## 作者已完成的修订

- 重写标题、摘要与引言，形成“工程问题—受限监督证据—结构先验—可检验问题”的连续叙事。
- 统一核心术语：`controlled sparse cycle-level supervision`、`SOH-label retention ratio`、`test cell`、`soft monotonicity constraint`、`trajectory consistency`。
- 重写方法衔接，区分完整特征窗口、被保留的数值标签与小批量内可用的有序对，避免将局部约束夸大为完整轨迹约束。
- 将结果重排为：全监督基准 → 标签预算曲线与代表电芯 → 扩展模型端点比较 → 轨迹一致性 → $2\times2$ 析因解释。
- 改写图注和表注，补足种子、重复次数、统计量、选择规则及箱线图含义。
- 重写结论，使其回收实验结论而不重复防御性限定。
- 在损失函数章节补入 M4 变化率连续性项：以预测 SOH 的离散二阶差分约束相邻变化率，并明确主 $2\times2$ 矩阵设 $\lambda_{\mathrm{rate}}=0$、M4 配置设为 0.05；因此不将该项误写为已由当前主结果验证。
- 重新编译并完成版式核验：21 页；无未解析引用、未定义控制序列或 overfull 文本框。

## 投稿前仍需作者确认的事项

1. **代码可用性**：请提供最终公开仓库、存档 DOI，或确认期刊允许的可用性措辞。当前稿件仍保留占位表述，不能作为最终投稿声明。
2. **目标期刊**：确认具体期刊后，需按其摘要长度、关键词数、声明格式、参考文献体例及图像规格做一次定稿适配。
3. **架构图可读性**：Fig. 1 的信息密度偏高。建议输出一张放大后的单栏版本：主流程置上，训练目标置下；将层参数移至图注或补充材料。
4. **源码清理**：主 `manuscript.tex` 的 `\iffalse ... \fi` 中仍存有旧结果和未证实扩展内容。它们未进入 PDF，但投稿前应由作者确认后移出主源码或归档，不能重新启用。
5. **外部验证**：本研究目前支持随机、分布式循环级标签保留的结论。连续前缀标签、周期标定、不同化学体系和独立电芯划分应作为后续增强实验。
